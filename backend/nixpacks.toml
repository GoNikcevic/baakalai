[phases.setup]
nixPkgs = ["nodejs_20", "python311", "gcc", "gnumake", "pkg-config"]

[phases.install]
cmds = ["cd backend && npm install --build-from-source"]

[phases.build]
cmds = ["echo 'No build step needed'"]

[start]
cmd = "cd backend && node server.js"
